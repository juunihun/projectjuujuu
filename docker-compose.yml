version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: ${NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY}
    container_name: juujuu_app
    restart: always
    env_file:
      - .env
    environment:
      # Override DB_HOST from .env (which might be localhost) to point to the db container
      - DB_HOST=db
    networks:
      - juujuu_network
    depends_on:
      db:
        condition: service_healthy

  db:
    image: mysql:8.0
    container_name: juujuu_db
    restart: always
    env_file:
      - .env
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-secret}
      MYSQL_DATABASE: ${DB_NAME:-juu_juu_shop}
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
      - ./scripts/schema.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - juujuu_network
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost" ]
      timeout: 20s
      retries: 10

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: juujuu_pma
    restart: always
    environment:
      PMA_HOST: db
      PMA_USER: root
      PMA_PASSWORD: ${DB_PASSWORD:-secret}
    ports:
      - "8080:80"
    networks:
      - juujuu_network
    depends_on:
      - db

  nginx:
    image: nginx:alpine
    container_name: juujuu_nginx
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - juujuu_network
    depends_on:
      - app

networks:
  juujuu_network:
    driver: bridge

volumes:
  db_data:
